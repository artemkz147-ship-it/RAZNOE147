package ru.filemaster.offline;

/**
 * Compatibility entry point kept for existing navigation.
 * The full editor now supports multiple pages, pen, highlighter, shapes, text and stamps.
 */
public class PdfAnnotateActivity extends PdfVisualEditorActivity {
}
