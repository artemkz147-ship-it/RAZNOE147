{
  "name": "vesely-natisk-3d",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "python3 tools/post_materialize_fixes.py && vite --host 0.0.0.0",
    "prebuild": "python3 tools/post_materialize_fixes.py",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "7.1.2"
  }
}
