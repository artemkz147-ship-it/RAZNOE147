# Offline Image Generator

APK packaging branch for a fully local Android image generator based on Local Diffusion / stable-diffusion.cpp.

Features: txt2img, img2img, inpainting/outpainting, LoRA, local .safetensors/.ckpt model import. Image generation is performed on-device without an external generation API.
